# Required for vision API (ingredients + weights from food photo)
OPENAI_API_KEY=sk-...

# Optional: path to CSV with nutrition per 100g (name, calories, total_fat, protein, carbohydrate)
# If set, backend will compute total calories and macros for the donut chart
# NUTRITION_CSV_PATH=./nutrition.csv
